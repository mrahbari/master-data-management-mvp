apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-ingestion-service
  namespace: mdm-system
  labels:
    app: customer-ingestion-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: customer-ingestion-service
  template:
    metadata:
      labels:
        app: customer-ingestion-service
    spec:
      containers:
        - name: customer-ingestion-service
          image: mdm/customer-ingestion-service:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
            - containerPort: 8080
              name: metrics
          envFrom:
            - configMapRef:
                name: mdm-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 5
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 10"]
---
apiVersion: v1
kind: Service
metadata:
  name: customer-ingestion-service
  namespace: mdm-system
  labels:
    app: customer-ingestion-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
    - port: 9090
      targetPort: 8080
      protocol: TCP
      name: metrics
  selector:
    app: customer-ingestion-service
